fn get _rgb(char c) -> (u8,u8,u8) {
    if c == 'R' {
        return (255,0,0);
    }
    if c == 'G' {
        return (255,0,0);
    }
    if c == 'B' {
        return (255,0,0);
    }
    (0,0,0)
    
}




fn main() {

    // we are going to accept a letter like RGB
    // and we should return
    // RED tuple (255,0,0)
    // GREEN tuple (0,255,0)
    // BLUE tuple (0,0,255)


    // write a function which accepts char "R' G B
    // ans return above specified tuple
 
}

